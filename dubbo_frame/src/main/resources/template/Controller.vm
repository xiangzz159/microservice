package ${groupId}.${module}.${layer}.controller;

import ${groupId}.frame.base.BaseController;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.com.study.frame.annotation.RequestMapping;
import org.springframework.web.bind.com.study.frame.annotation.RequestMethod;

/**
* ${modelname}controller
* Created by Yerik on ${ctime}.
*/
@Controller
@RequestMapping("/manage")
@Api(value = "${modelname}控制器", description = "${modelname}管理")
public class ${model}Controller extends BaseController {

private static final Logger LOGGER = LoggerFactory.getLogger(${model}Controller.class);



}